version: '3.8'
services:
  app:
    image: node:lts
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    environment:
      - CI=true
      - WATCHPACK_POLLING=true
    command: npm start